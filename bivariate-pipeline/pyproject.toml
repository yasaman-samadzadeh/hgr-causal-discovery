# pyproject.toml
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hgrlingam"
version = "0.1"

[tool.setuptools.packages.find]   # <-- tell setuptools where to look
where = ["src"]
